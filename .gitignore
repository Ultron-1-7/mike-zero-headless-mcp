# ---- 虚拟环境 / 构建产物 ----
.venv/
venv/
build/
dist/
*.egg-info/
__pycache__/
*.py[cod]

# ---- 运行时数据 ----
# 作业注册表、日志、SPI 归档、诊断图；含本机绝对路径，不入库
state/

# ---- 官方算例副本（重要：DHI 版权，禁止分发）----
# demo/ 用来放你从 MIKE Zero 安装目录复制过来的官方算例；
# 这些文件属于 DHI，随包分发会侵权，所以整体忽略。
# 自己复现实验时把算例拷进来即可（见 README「冒烟 / 集成测试」）。
demo/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
